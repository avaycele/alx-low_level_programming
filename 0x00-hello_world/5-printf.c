#include <stdio.h>
/**
 *  * main - Entry point
 *
 * This function prints a formatted string to the standard output.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
